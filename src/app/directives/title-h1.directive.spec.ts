import { TitleH1Directive } from './title-h1.directive';

describe('TitleH1Directive', () => {
  it('should create an instance', () => {
    const directive = new TitleH1Directive();
    expect(directive).toBeTruthy();
  });
});
